import { LicensePlateFormatDirective } from './license-plate-format.directive';

describe('LicensePlateFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new LicensePlateFormatDirective();
    expect(directive).toBeTruthy();
  });
});
